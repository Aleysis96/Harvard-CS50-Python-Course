# Calculator that also reads decimals and does not have a variable for the result
# Just change to "int" to just read integers numbers
x = float(input("First number = "))
y = float(input("Second number = "))

print("Round result = ", round(x + y))

