def main():
    coordinates = (42.376, -71.115)
    latitude, longitude = coordinates # Unpack the tuple into two separate variables: latitude(0) and longitude(1)
    print(f"Latitude: {latitude}") # Print the latitude value using formatted output
    print(f"Longitude: {longitude}") # Print the longitude value using formatted output

main() # Call the main function to execute the program

