students = ["Harry", "Ron", "Hermione"] # declare a list of values

for i in range(len(students)): # len is the number of values in a list
    print(i + 1, students[i]) # rank the values from the list by starting in 1
